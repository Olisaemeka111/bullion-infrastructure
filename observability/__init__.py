"""Observability: metrics, SLOs, alert rules, and dashboards.

The control plane is instrumented with Prometheus-style metrics; alert rules and
Grafana dashboards are shipped as code alongside it so monitoring is provisioned
with the same review/audit path as everything else.
"""
